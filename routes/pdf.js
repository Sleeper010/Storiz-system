import { Router } from 'express';

const router = Router();

// Placeholder — will be implemented in Phase 6
router.post('/generate', (req, res) => {
  res.json({ message: 'PDF generation pending' });
});

export default router;
