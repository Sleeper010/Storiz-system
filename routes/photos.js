import { Router } from 'express';

const router = Router();

// Placeholder — will be implemented in Phase 5
router.post('/upload', (req, res) => {
  res.json({ message: 'Photo upload pending' });
});

export default router;
